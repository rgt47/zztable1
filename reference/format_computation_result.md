# Format Computation Result

Ensures computation results are properly formatted for display.

## Usage

``` r
format_computation_result(result)
```

## Arguments

- result:

  Raw computation result

## Value

Formatted character result
