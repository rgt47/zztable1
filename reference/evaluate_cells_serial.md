# Evaluate cells serially (default method)

Evaluates cells one at a time in the main process. This is the default
when parallel processing is not beneficial.

## Usage

``` r
evaluate_cells_serial(blueprint)
```

## Arguments

- blueprint:

  Table1Blueprint object

## Value

Data frame with evaluated cell contents
