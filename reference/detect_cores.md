# Detect number of available CPU cores

Platform-aware core detection with sensible defaults. Respects
environment variables for cluster environments.

## Usage

``` r
detect_cores()
```

## Value

Integer number of cores (minimum 1)
