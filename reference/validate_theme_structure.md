# Validate theme structure

Checks that a theme object has all required fields.

## Usage

``` r
validate_theme_structure(theme_obj)
```

## Arguments

- theme_obj:

  Theme object to validate

## Value

Invisibly returns TRUE if valid
