# Safe Function Execution

Executes an expression with error handling and returns fallback on error

## Usage

``` r
safe_eval(expr, fallback = "[Error]")
```

## Arguments

- expr:

  Expression to execute

- fallback:

  Value to return on error

## Value

Result of expression or fallback value
