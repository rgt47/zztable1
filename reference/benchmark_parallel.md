# Benchmark parallel vs serial processing

Runs a quick benchmark to determine if parallel processing would be
beneficial for the current system.

## Usage

``` r
benchmark_parallel(num_test_cells = 1000)
```

## Arguments

- num_test_cells:

  Number of test cells (default: 1000)

## Value

List with benchmark results
