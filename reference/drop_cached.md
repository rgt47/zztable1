# Drop a Cached Result

Removes a single entry from the blueprint cache, used when the cell
occupying that position is replaced or removed.

## Usage

``` r
drop_cached(blueprint, cache_key)
```

## Arguments

- blueprint:

  Table1Blueprint object

- cache_key:

  Character string with cache key

## Value

NULL, invisibly
