# Parse Formula and Analyze Dimensions (Combined)

Parse Formula and Analyze Dimensions (Combined)

## Usage

``` r
parse_and_analyze(
  formula,
  data,
  strata,
  missing,
  size,
  totals,
  pvalue,
  layout,
  footnotes,
  block = NULL,
  collapse_binary = FALSE
)
```
