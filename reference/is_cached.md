# Check Blueprint Cache Status

Determines if a statistical result is cached in the blueprint.

## Usage

``` r
is_cached(blueprint, cache_key)
```

## Arguments

- blueprint:

  Table1Blueprint object

- cache_key:

  Character string with cache key

## Value

Logical indicating if result is cached
