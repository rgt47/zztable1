# Apply Theme to Blueprint

Applies a theme configuration to a table1_blueprint object. Updates
metadata with theme information.

## Usage

``` r
apply_theme(blueprint, theme)
```

## Arguments

- blueprint:

  Table1Blueprint object

- theme:

  Theme name (character) or theme object (list)

## Value

Modified blueprint with theme applied
