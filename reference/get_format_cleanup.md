# Get format-specific cleanup lines

Get format-specific cleanup lines

## Usage

``` r
get_format_cleanup(theme, format)
```

## Arguments

- theme:

  Theme configuration

- format:

  Output format

## Value

Character vector with cleanup commands
