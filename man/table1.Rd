% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zztable1.R
\name{table1}
\alias{table1}
\alias{table1.formula}
\alias{latex}
\title{Create a summary table of baseline characteristics}
\usage{
table1(form, data, ...)

\method{table1}{formula}(
  form,
  data,
  strata = NULL,
  block = NULL,
  missing = FALSE,
  pvalue = TRUE,
  size = FALSE,
  totals = FALSE,
  fname = "table1",
  layout = "console",
  ...
)

latex(tab, digits = 3, fname = "table0", theme = theme_nejm, ...)
}
\arguments{
\item{form}{Formula specifying the grouping and variables to summarize (e.g., arm ~ sex + age)}

\item{data}{Dataframe containing the variables}

\item{...}{Additional parameters passed to kable}

\item{strata}{Optional stratification variable}

\item{block}{Deprecated parameter}

\item{missing}{Logical; whether to show missing values}

\item{pvalue}{Logical; whether to show p-values}

\item{size}{Logical; whether to show group sizes}

\item{totals}{Logical; whether to show totals}

\item{fname}{Filename for the output LaTeX file}

\item{layout}{Output format (e.g., "console")}

\item{tab}{Table created by table1()}

\item{digits}{Number of digits to display for numeric values}

\item{theme}{List with foreground and background colors for table rows}
}
\value{
A dataframe containing the summary table

A kable object representing the formatted table
}
\description{
This is the main function used to create summary tables for clinical trial data.
It summarizes baseline characteristics by treatment groups and calculates
appropriate statistics and p-values for comparisons.

Formats and exports a table1 object to LaTeX format with customizable styling.
}
\section{Methods (by class)}{
\itemize{
\item \code{table1(formula)}: Method for formula interface

}}
\section{Functions}{
\itemize{
\item \code{latex()}: interprets formula and yields publication tables

}}
\examples{
# Create a sample dataset
set.seed(123)
trial_data <- data.frame(
  arm = factor(rep(c("Treatment", "Placebo"), each = 50)),
  age = rnorm(100, mean = 45, sd = 15),
  sex = factor(sample(c("Male", "Female"), 100, replace = TRUE)),
  bmi = rnorm(100, mean = 26, sd = 5),
  site = factor(sample(c("Site1", "Site2", "Site3"), 100, replace = TRUE))
)

# Create a simple summary table
table1(form = arm ~ sex + age + bmi, data = trial_data)

# Create a table with totals
table1(form = arm ~ sex + age + bmi, data = trial_data, totals = TRUE)

# Create a table without p-values
table1(form = arm ~ sex + age + bmi, data = trial_data, pvalue = FALSE)

# Create a table with stratification
table1(form = arm ~ sex + age, data = trial_data, strata = "site")

# Create a sample table
\dontrun{
set.seed(123)
trial_data <- data.frame(
  arm = factor(rep(c("Treatment", "Placebo"), each = 50)),
  age = rnorm(100, mean = 45, sd = 15),
  sex = factor(sample(c("Male", "Female"), 100, replace = TRUE))
)
tab <- table1(form = arm ~ age + sex, data = trial_data)

# Export to LaTeX
latex(tab, digits = 2, fname = "my_table")

# Use a custom theme
my_theme <- list(
  foreground = c("black", "black", "black", "black", "black"),
  background = c("#f0f0f0", "white", "#f0f0f0", "white", "white")
)
latex(tab, theme = my_theme)
}

}
