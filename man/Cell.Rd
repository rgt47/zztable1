% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cells.R
\name{Cell}
\alias{Cell}
\title{Create Cell Object with Validation (User Interface)}
\usage{
Cell(
  type,
  content = NULL,
  data_subset = NULL,
  computation = NULL,
  dependencies = NULL,
  format = list(),
  cached_result = NULL,
  footnote_number = NULL,
  footnote_text = NULL
)
}
\arguments{
\item{type}{Character string specifying cell type. Must be one of:
"content", "computation", "separator"}

\item{content}{Static text content (required for "static" type)}

\item{data_subset}{R expression for data subsetting (required for "computation")}

\item{computation}{R expression for calculation (required for "computation")}

\item{dependencies}{Character vector of variable dependencies}

\item{format}{Named list of formatting options}

\item{cached_result}{Cached computation result (for performance)}

\item{footnote_number}{Integer footnote number (for footnote cells)}

\item{footnote_text}{Character footnote text (for footnote cells)}
}
\value{
Validated Cell object of class "cell"
}
\description{
User-facing constructor that provides comprehensive input validation
and creates properly validated Cell objects.
}
\examples{
# Static cell
Cell(type = "content", content = "Age (years)")

# Computation cell
Cell(
  type = "computation",
  data_subset = expression(data$age[data$group == "Treatment"]),
  computation = expression(paste0(round(mean(x), 1), " +/- ", round(sd(x), 1))),
  dependencies = c("data", "age", "group")
)

# Footnote cell
Cell(
  type = "footnote", footnote_number = 1,
  footnote_text = "Age measured at baseline"
)

}
