% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_rows.R
\name{add_summary_rows}
\alias{add_summary_rows}
\title{Add Per-Group Summary Rows to a Blueprint}
\usage{
add_summary_rows(
  blueprint,
  fns,
  columns = NULL,
  side = "bottom",
  groups = NULL,
  fmt_fn = NULL
)
}
\arguments{
\item{blueprint}{A table1_blueprint object}

\item{fns}{A named list of aggregation functions. Each function
receives a numeric vector and returns a single value or formatted
string. Names become the summary row labels.}

\item{columns}{Integer or character vector of columns to
summarise. Defaults to all data columns (excludes the variable
name column and the p-value column).}

\item{side}{Where to place summary rows: \code{"bottom"} (default)
or \code{"top"} of each group.}

\item{groups}{Character vector of row group names to summarise.
Defaults to all groups.}

\item{fmt_fn}{Optional formatting function applied to each
computed value before display. Receives a numeric value,
returns a character string.}
}
\value{
The blueprint, modified in place.
}
\description{
Defines aggregation functions that are computed at render time for
each row group in the table. A row group is a set of contiguous
rows belonging to the same variable (for Table 1 layouts) or the
same stratum.
}
